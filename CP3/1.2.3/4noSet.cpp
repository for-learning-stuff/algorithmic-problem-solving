// Read and print unique chars, sorted
#include <bits/stdc++.h>
using namespace std;

#define ALL(x) x.begin(), x.end()
#define UNIQUE(c) (c).resize(unique(ALL(c)) - (c).begin())

int main(){
    s.insert(n); // Sets automatically sort    

    int a[] = {1, 2, 2, 2, 3, 3, 2, 2, 1};
    vector(a, a + 9); // 9 to point off end
    for(int i: s){
        cout << i;
    }
}